# Plan: ai-0002 — STT

## Steps
1) 接入 multipart 与文件大小限制
2) 实现 OpenAI STT adapter（`openai` SDK）
3) 实现 `/api/stt/transcribe`（provider/model 校验、缺 key 错误码）
4) 本地验证：缺 key 路径；build 通过

## Verification
- `cd apps/api; npm run build`
- `curl -F provider=openai -F model=whisper-1 -F audio=@<file> http://localhost:808x/api/stt/transcribe`

