# Plan: deploy-0003 — VPS Deploy Guide

## Steps
1) 编写部署文档 `deploy/vps_backend.md`（包含 DNS、SSH、安全、Docker、Nginx、certbot、运行与回滚）
2) 提供 Nginx 配置模板 `deploy/nginx/story-api.suenbeya.com.conf`
3) 提供 docker compose 模板 `deploy/docker-compose.story-api.yml`
4) 落盘 report/review

