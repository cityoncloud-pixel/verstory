# Plan: ai-0001 — Model registry

## Steps
1) 新增 server 框架（Fastify）并接入 CORS
2) 实现 provider registry（代码内配置 + defaults）
3) 实现 `/api/models` 与 `/api/config/check`
4) 增加 `apps/api/.env.example`
5) 本地验证：`npm run build`；`npm run dev` + curl

