# Plan: mvp-0002 — Recording + IndexedDB Storage

## Steps
1) 引入 IndexedDB 封装（使用轻量 `idb`），建立 v1 schema（projects/segments）
2) 改造项目管理：从 IndexedDB 读取/写入项目；保留 activeProjectId 在 localStorage
3) 实现录音控制：MediaRecorder 开始/停止；停止时生成 blob，计算时长，写入 IndexedDB
4) 实现片段列表：按 projectId 查询 segments，展示并回放；支持删除片段
5) 验证：手工走通“录两段→刷新→回放”；`npm run build` 通过

## Files
- `apps/web/src/*`（新增/修改：db、录音逻辑、UI）
- `specs/mvp-0002.spec.md`
- `plans/mvp-0002.plan.md`
- `reviews/mvp-0002.review.md`
- `reports/mvp-0002.report.md`

## Verification Commands
- `cd apps/web; npm run build`

