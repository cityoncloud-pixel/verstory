# Plan: mvp-0003 — Mock Providers

## Steps
1) 新增 provider 接口与 mock 实现（转写/整理），确保确定性输出
2) 在 UI 中接入：按钮触发生成并展示结果
3) 使用 localStorage 持久化转写与故事（按 projectId 存储）
4) 验证：`npm run build`；手工验证“点击生成→刷新恢复→重复生成一致”

## Files
- `apps/web/src/providers/*`（新增）
- `apps/web/src/App.tsx`（接入与持久化）
- `specs/mvp-0003.spec.md`
- `plans/mvp-0003.plan.md`
- `reviews/mvp-0003.review.md`
- `reports/mvp-0003.report.md`

## Verification Commands
- `cd apps/web; npm run build`

