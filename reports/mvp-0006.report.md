# Report: mvp-0006 — Cloud Storage (Postgres + R2) + Auth + Responsive UI

## Status
In progress (responsive layout v2 implemented; backend/cloud storage wiring pending)

## Owner Inputs Confirmed
- Storage: Postgres + Cloudflare R2
- Auth: email/password, pre-provisioned users only
- Domains: `story.suenbeya.com` (web) + `story-api.suenbeya.com` (api)

## Required Runtime Config (no secrets in repo)
- `DATABASE_URL`
- `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `COOKIE_DOMAIN`
- `R2_ENDPOINT`, `R2_BUCKET`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`

## Next Execution Checkpoints
1) Auth + DB migrations working locally
2) R2 presigned upload + playback working
3) Frontend: verify mobile/tablet/desktop layout + per-panel status

## Progress (2026-05-08)
- Implemented workspace layout per `verstory_workspace_layout_spec_v2.md`:
  - Sticky `TopBar` with project selector + menu (removed right-side action stack)
  - Fixed `BottomWorkflowTabs` for 录音/转写/整理稿
  - Single-stage content render (tab == content)
  - Removed build display and the old “project sidebar” block that caused mobile first-screen waste

