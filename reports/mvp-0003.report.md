# Report: mvp-0003

## Status
DONE

## Verification
PARTIAL (build PASS; runtime mock 生成需手工确认确定性)

## Commands
- `cd apps/web; npm run build` (PASS)

## Manual Smoke Test
1) `cd apps/web; npm run dev`
2) 选择一个有录音片段的项目（若无，先录两段）
3) 点击“转写（mock）”→ 出现原始转写全文
4) 点击“生成故事（mock）”→ 出现整理后故事（含整理原则）
5) 刷新页面 → 文本仍可恢复；重复点击应输出一致（确定性）

## Changes
- 新增 mock providers：
  - `apps/web/src/providers/transcription.ts`
  - `apps/web/src/providers/polish.ts`
- UI 接入生成与展示（`apps/web/src/App.tsx`）
- 文本持久化：按 projectId 写入 localStorage（避免 IndexedDB schema 变更）

## Next Step
- 执行 `mvp-0004`：导出 Markdown + UI 体验补齐（按钮状态/错误提示/布局微调）。

