# Report: mvp-0002

## Status
DONE

## Verification
PARTIAL (build PASS; runtime recording requires manual smoke test)

## Commands
- `cd apps/web; npm run build` (PASS)

## Manual Smoke Test (Owner/runner)
1) `cd apps/web; npm run dev`
2) 打开页面 → 新建项目 → 开始录音 → 停止并保存 → 继续录音 → 停止并保存
3) 片段列表中应出现 2 段音频，可回放
4) 刷新页面 → 仍可回放（IndexedDB 恢复）

## Changes
- 新增 IndexedDB v1（`apps/web/src/db.ts`），存储 `projects` / `segments`（含 blob）
- 项目列表改为从 IndexedDB 读取/写入；activeProjectId 仍存 localStorage
- 实现 MediaRecorder 分段录音与保存；片段列表支持回放与删除

## Risks / Notes
- `MediaRecorder` mimeType 受浏览器支持影响；已做候选类型兜底，但仍可能在特定浏览器失败
- 音频时长通过加载 metadata 获取；极少数情况下可能返回 0

## Next Step
- 执行 `mvp-0003`：mock 转写与 mock 故事整理 provider（要求离线确定性输出）。

