name: Deploy story.suenbeya.com (Hostinger FTP)

on:
  push:
    branches: [main]
    paths:
      - "apps/web/**"
      - ".github/workflows/deploy-hostinger.yml"
  workflow_dispatch: {}

concurrency:
  group: hostinger-story-deploy
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/web/package-lock.json

      - name: Install
        working-directory: apps/web
        run: npm install --no-audit --no-fund

      - name: Build
        working-directory: apps/web
        env:
          VITE_BUILD_ID: ${{ github.sha }}
        run: npm run build

      - name: Write build marker
        working-directory: apps/web
        run: |
          echo "${GITHUB_SHA}" > dist/build.txt

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.HOSTINGER_FTP_HOST }}
          username: ${{ secrets.HOSTINGER_FTP_USER }}
          password: ${{ secrets.HOSTINGER_FTP_PASS }}
          port: ${{ secrets.HOSTINGER_FTP_PORT }}
          protocol: ftps
          local-dir: apps/web/dist/
          # This FTP user is scoped to the story subdirectory already; deploy to root to avoid path nesting.
          server-dir: /
          dangerous-clean-slate: true
